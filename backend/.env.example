# ========================================
# START OF CODE
# ========================================

# Template for backend/.env. Copy this file; never commit the real .env.
FIREBASE_WEB_API_KEY=paste_web_api_key_here
# Required on Render. Locally the API can use firebase/serviceAccount.json instead.
# Paste as one line: python -c "import json; print(json.dumps(json.load(open('../firebase/serviceAccount.json.json'))))"
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# ========================================
# END OF CODE
# ========================================
